-->go to --> https://start.spring.io/?utm_source=copilot.com
-->fill all the details and download file
-->unzip the file in filemanager
-->open eclipse and import the file
-->change the pom.xml
-->create classes inside sc/main/java
-->run the class(SampleApplication.java) inside the com.mphasis.learning.sample inside src/main/java (right click, run as java)
-->now go to browser and paste the link in browser (http://localhost:8080/swagger-ui/index.html#/user-controller/getAllUsers)
-->after opening that page(we will all that page as swagger which is simpler way as compared to the postman) we can add, get, delete, update users(CRUD operations)


So Swagger is just a UI client that makes it easy to send these HTTP requests. Every time you clicked “Execute,” Swagger was sending an HTTP request with a method (GET/POST/PUT/DELETE) to your REST API.